name: Deploy App

# Only run manually for now
on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Echo Deploy
        run: echo "Deployment placeholder - App is hosted on Streamlit Cloud"
